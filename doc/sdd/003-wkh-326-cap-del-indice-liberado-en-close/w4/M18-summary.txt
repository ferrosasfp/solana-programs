  47 passing (5s)
  7 failing
    1) 13. close with escrowIndex=null confirms and does NOT create the index account (AC-2)
    2) 21. close with and without the index: report computeUnitsConsumed
    3) D1. dust donated to the vault after the release no longer bricks the close: it is swept to the sender
    4) D1b. the same holds after a refund: dust does not turn the close into a dead end
    5) E2. an account already sitting on chain as Released is still closable (its byte was not re-pointed)
    6) E3. an account already sitting on chain as Refunded is still closable too
    7) 8. close after release returns rent + vault to sender, and the same seeds are reusable via a fresh deposit (anti-revival)
  1) escrow-index — enumerable per-sender escrow index (HU-SOL-20)
  2) escrow-index — enumerable per-sender escrow index (HU-SOL-20)
  3) escrow-window — the custody window and the status guard
  4) escrow-window — the custody window and the status guard
  5) escrow-window — the custody window and the status guard
  6) escrow-window — the custody window and the status guard
  7) escrow — WasiAI trustless USDC escrow (anchor-bankrun)
