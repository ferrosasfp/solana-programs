# ── PASADA 1 de 2 · baseline 54 ─────────────────────────────────────────────────
# Esta salida NO es la que describe la prosa de doc/mutation-run.md, que narra la PASADA 2.
# Fuente: commit bdd9d92 — lib.rs md5 2e56fb6a09006c304c2132d5b42eebf6, tests/escrow-index.ts con
# 24 tests, el 14 asertando `to.be.at.most(1)` y sin el 13c.
# La PASADA 2 (baseline 55, sobre el arbol del fix-pack 0fdec52: el 14 con `to.equal(0)` y el 13c
# nuevo) esta registrada en doc/mutation-run.md, con el conjunto de tests muertos de cada mutante y
# los md5 de referencia verificados despues de cada restauracion. No se re-corrio para escribir esta
# cabecera.
# Check de una linea, sin abrir nada mas: passing + failing = 54 en los cinco .txt de esta carpeta.
# Si algun archivo sumara 55, ese seria de la PASADA 2 y esta cabecera estaria mal puesta.
# ── salida cruda de la corrida, sin editar, debajo ────────────────────────────
  52 passing (4s)
  2 failing
    1) 12. close removes exactly the entry of the escrow it closes and leaves the rest in order (AC-1)
    2) 18. close with an index that does not contain the id confirms and leaves entries alone (AC-7)
  1) escrow-index — enumerable per-sender escrow index (HU-SOL-20)
  2) escrow-index — enumerable per-sender escrow index (HU-SOL-20)
